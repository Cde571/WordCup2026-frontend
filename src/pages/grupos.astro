---
import MainLayout from "../components/layout/MainLayout.astro";
import GroupStagePicker from "../components/groups/GroupStagePicker.tsx";
---

<MainLayout title="Mundial 2026 – Predicciones de grupos">
  <section class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
    <div>
      <h1 class="font-bebas text-3xl md:text-4xl text-white tracking-[0.16em]">
        Predicciones – Fase de grupos
      </h1>
      <p class="mt-2 text-xs md:text-sm text-slate-300 max-w-xl leading-relaxed">
        Elige qué selecciones serán 1.º, 2.º y 3.º en cada grupo del Mundial 2026.
        Estos puestos definirán los cruces de la fase eliminatoria y tu puntaje
        a lo largo del torneo.
      </p>
    </div>

    <div class="flex flex-col items-start md:items-end gap-2">
      <span class="inline-flex items-center gap-2 rounded-full border border-yellow-400/60 bg-yellow-500/10 px-3 py-1.5">
        <span class="inline-block h-2 w-2 rounded-full bg-yellow-400 animate-pulse"></span>
        <span class="text-[11px] md:text-xs font-semibold text-yellow-200">
          Progreso de grupos: próximamente desde backend
        </span>
      </span>
      <p class="text-[11px] text-slate-400">
        Las predicciones se guardarán en tu cuenta cuando el login esté activo.
      </p>
    </div>
  </section>

  <section class="mt-6">
    <GroupStagePicker client:load />
  </section>
</MainLayout>
