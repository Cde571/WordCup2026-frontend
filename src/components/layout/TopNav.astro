<nav class="sticky top-0 z-50 border-b border-slate-800 bg-gradient-to-b from-slate-950/95 via-slate-900/90 to-slate-900/85 backdrop-blur">
  <div class="max-w-7xl mx-auto px-4 py-3 flex items-center justify-between gap-4">

    <!-- Logo -->
    <a href="/" class="flex-shrink-0">
      <div class="flex items-center gap-3 rounded-xl bg-slate-950/90 backdrop-blur border-2 border-yellow-500/30 px-4 py-2 shadow-lg hover:border-yellow-500/60 transition-all duration-300">
        <!-- Badge -->
        <div class="relative">
          <div class="absolute inset-0 bg-yellow-400 rounded-lg blur-sm opacity-40"></div>
          <div class="relative bebas text-2xl font-black text-yellow-400 px-2.5 py-0.5 bg-slate-900 rounded-lg border-2 border-yellow-400">
            WC26
          </div>
        </div>

        <!-- Texto -->
        <div class="hidden sm:flex flex-col leading-tight">
          <span class="bebas text-lg font-bold text-white tracking-widest">
            MUNDIAL 2026
          </span>
          <span class="text-[10px] text-slate-400 font-medium">
            Predicciones & Brackets
          </span>
        </div>
      </div>
    </a>

    <!-- Navegación desktop -->
    <div class="hidden lg:flex items-center gap-1 text-sm tracking-wider uppercase">
      <a href="/" class="px-3 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800/80 transition-colors">Inicio</a>
      <a href="/grupos" class="px-3 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800/80 transition-colors">Grupos</a>
      <a href="/bracket" class="px-3 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800/80 transition-colors">Bracket</a>
      <a href="/partidos" class="px-3 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800/80 transition-colors">Partidos</a>
      <a href="/equipos" class="px-3 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800/80 transition-colors">Equipos</a>
      <a href="/ranking" class="px-3 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800/80 transition-colors">Ranking</a>
    </div>

    <!-- Acciones derecha -->
    <div class="flex items-center gap-3">
      <!-- Beta badge -->
      <span class="hidden md:inline-flex items-center gap-1.5 rounded-full border border-emerald-400/60 bg-emerald-500/10 px-3 py-1.5">
        <span class="inline-block h-2 w-2 rounded-full bg-emerald-400 animate-pulse"></span>
        <span class="text-xs font-semibold text-emerald-200">
          Beta activa
        </span>
      </span>

      <!-- Perfil (solo desktop/tablet) -->
      <a
        href="/perfil"
        class="hidden md:inline-flex px-4 py-2 rounded-full bg-yellow-400 text-slate-900 text-sm font-semibold hover:bg-yellow-300 transition-colors shadow-md hover:shadow-lg"
      >
        Mi perfil
      </a>

      <!-- Mobile: botón Predicciones + hamburguesa (solo móvil) -->
      <div class="flex items-center gap-2 md:hidden">
        <a
          href="/grupos"
          class="px-4 py-2 rounded-full bg-yellow-400 text-slate-900 text-sm font-semibold whitespace-nowrap"
        >
          Predicciones
        </a>

        <button
          id="menuBtn"
          type="button"
          class="inline-flex h-10 w-10 items-center justify-center rounded-lg bg-slate-800/80 hover:bg-slate-700/80 transition-colors border border-slate-700"
          aria-label="Abrir menú"
          aria-controls="mobileMenu"
          aria-expanded="false"
        >
          <svg id="iconOpen" class="w-5 h-5 text-slate-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
          <svg id="iconClose" class="hidden w-5 h-5 text-slate-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Menú móvil (panel) -->
  <div id="mobileMenu" class="hidden md:hidden">
    <!-- Backdrop -->
    <div id="menuBackdrop" class="fixed inset-0 z-40 bg-black/60"></div>

    <!-- Panel -->
    <div class="fixed right-3 top-[72px] z-50 w-[min(92vw,360px)] rounded-2xl border border-slate-700 bg-slate-950/95 p-3 shadow-2xl">
      <div class="flex items-center justify-between px-1 pb-2">
        <p class="text-[11px] font-semibold tracking-[0.18em] text-white/70 uppercase">
          Navegación
        </p>
        <a
          href="/perfil"
          class="rounded-full bg-yellow-400 px-3 py-2 text-[11px] font-bold text-slate-900 hover:bg-yellow-300"
        >
          Mi perfil
        </a>
      </div>

      <div class="grid gap-1 text-sm uppercase tracking-wider">
        <a href="/" class="mobileLink px-3 py-2 rounded-xl text-slate-200 hover:bg-slate-800/60">Inicio</a>
        <a href="/grupos" class="mobileLink px-3 py-2 rounded-xl bg-yellow-400 text-slate-900 font-semibold hover:bg-yellow-300">Grupos</a>
        <a href="/bracket" class="mobileLink px-3 py-2 rounded-xl text-slate-200 hover:bg-slate-800/60">Bracket</a>
        <a href="/partidos" class="mobileLink px-3 py-2 rounded-xl text-slate-200 hover:bg-slate-800/60">Partidos</a>
        <a href="/equipos" class="mobileLink px-3 py-2 rounded-xl text-slate-200 hover:bg-slate-800/60">Equipos</a>
        <a href="/ranking" class="mobileLink px-3 py-2 rounded-xl text-slate-200 hover:bg-slate-800/60">Ranking</a>
      </div>

      <p class="mt-3 px-2 text-[11px] text-white/50">
        Toca afuera para cerrar.
      </p>
    </div>
  </div>
</nav>

<script is:inline type="module">
  const btn = document.getElementById("menuBtn");
  const menu = document.getElementById("mobileMenu");
  const backdrop = document.getElementById("menuBackdrop");
  const iconOpen = document.getElementById("iconOpen");
  const iconClose = document.getElementById("iconClose");

  function setOpen(open) {
    if (!btn || !menu) return;

    btn.setAttribute("aria-expanded", String(open));

    if (open) {
      menu.classList.remove("hidden");
      iconOpen?.classList.add("hidden");
      iconClose?.classList.remove("hidden");
      document.body.style.overflow = "hidden";
    } else {
      menu.classList.add("hidden");
      iconOpen?.classList.remove("hidden");
      iconClose?.classList.add("hidden");
      document.body.style.overflow = "";
    }
  }

  function toggle() {
    const open = btn?.getAttribute("aria-expanded") === "true";
    setOpen(!open);
  }

  btn?.addEventListener("click", toggle);
  backdrop?.addEventListener("click", () => setOpen(false));

  // Cierra al tocar un link
  menu?.querySelectorAll(".mobileLink").forEach((a) => {
    a.addEventListener("click", () => setOpen(false));
  });

  // Cierra con ESC
  window.addEventListener("keydown", (e) => {
    if (e.key === "Escape") setOpen(false);
  });
</script>
